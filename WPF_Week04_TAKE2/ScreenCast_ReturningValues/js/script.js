// Screencast : Returning Values



var total = calcArea(30, 20); //( , ) are arguements


function calcArea(w, h){  //( , ) Parameters 
	var area = w * h;
	return area; //function is spitting the information out... and this happens where the function is envoked
}
console.log(total);